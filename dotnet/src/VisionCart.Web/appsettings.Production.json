{
  "//": "Production overrides. NO SECRETS IN THIS FILE — it is committed to source control.",
  "//secrets": "Set every value below through environment variables on the host. ASP.NET Core maps a double underscore to a colon: ConnectionStrings__DefaultConnection, Email__Password, Payments__StripeSecretKey. See docs/07-deployment.md.",

  "ConnectionStrings": {
    "DefaultConnection": ""
  },

  "Seed": {
    "//": "Off in production. The first administrator is created once, deliberately, by setting Seed__Enabled=true with credentials for a single boot and then removing them.",
    "Enabled": false,
    "AdminEmail": "",
    "AdminPassword": "",
    "OpticianEmail": "",
    "OpticianPassword": ""
  },

  "Email": {
    "//": "Driver 'log' writes mail to the log instead of sending it. That is a development convenience and a production outage.",
    "Driver": "smtp",
    "Host": "",
    "Port": 587,
    "Username": "",
    "Password": "",
    "UseSsl": true
  },

  "FileLog": {
    "Enabled": true,
    "Directory": "logs",
    "MinimumLevel": "Information",
    "RetainDays": 14,
    "MaxBytes": 20971520
  },

  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "VisionCart": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },

  "AllowedHosts": "*"
}
