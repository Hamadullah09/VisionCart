{
  "name": "visioncart-assets",
  "private": true,
  "type": "module",
  "description": "Build-time asset generation for the .NET application. Node is never required to run or deploy VisionCart — these regenerate committed assets when the source geometry changes.",
  "scripts": {
    "frames": "node generate-frame-assets.mjs",
    "tryon": "node fetch-tryon-assets.mjs"
  },
  "devDependencies": {
    "@mediapipe/tasks-vision": "^0.10.21",
    "sharp": "^0.34.0"
  }
}
